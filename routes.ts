/**
 * Public routes
 * @type {string[]}
 */

export const publicRoutes = [
  "/",
  "/login",
  "/dashboard",
  "/dashboard/*"
];

export const authRoutes = ["/login"];

export const apiAuthPrefix = "/api/auth";